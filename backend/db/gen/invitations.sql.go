// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: invitations.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createInvitation = `-- name: CreateInvitation :one

INSERT INTO invitations (org_id, scheme_id, unit_id, email, full_name, role, token, expires_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, org_id, scheme_id, unit_id, email, full_name, role, token, status, expires_at, created_at
`

type CreateInvitationParams struct {
	OrgID     uuid.UUID   `json:"org_id"`
	SchemeID  uuid.UUID   `json:"scheme_id"`
	UnitID    pgtype.UUID `json:"unit_id"`
	Email     string      `json:"email"`
	FullName  string      `json:"full_name"`
	Role      string      `json:"role"`
	Token     string      `json:"token"`
	ExpiresAt time.Time   `json:"expires_at"`
}

// backend/db/queries/invitations.sql
func (q *Queries) CreateInvitation(ctx context.Context, arg CreateInvitationParams) (Invitation, error) {
	row := q.db.QueryRow(ctx, createInvitation,
		arg.OrgID,
		arg.SchemeID,
		arg.UnitID,
		arg.Email,
		arg.FullName,
		arg.Role,
		arg.Token,
		arg.ExpiresAt,
	)
	var i Invitation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.SchemeID,
		&i.UnitID,
		&i.Email,
		&i.FullName,
		&i.Role,
		&i.Token,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const getInvitationByID = `-- name: GetInvitationByID :one
SELECT id, org_id, scheme_id, unit_id, email, full_name, role, token, status, expires_at, created_at FROM invitations WHERE id = $1 LIMIT 1
`

func (q *Queries) GetInvitationByID(ctx context.Context, id uuid.UUID) (Invitation, error) {
	row := q.db.QueryRow(ctx, getInvitationByID, id)
	var i Invitation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.SchemeID,
		&i.UnitID,
		&i.Email,
		&i.FullName,
		&i.Role,
		&i.Token,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const getInvitationByToken = `-- name: GetInvitationByToken :one
SELECT id, org_id, scheme_id, unit_id, email, full_name, role, token, status, expires_at, created_at FROM invitations WHERE token = $1 LIMIT 1
`

func (q *Queries) GetInvitationByToken(ctx context.Context, token string) (Invitation, error) {
	row := q.db.QueryRow(ctx, getInvitationByToken, token)
	var i Invitation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.SchemeID,
		&i.UnitID,
		&i.Email,
		&i.FullName,
		&i.Role,
		&i.Token,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}

const listInvitationsByOrg = `-- name: ListInvitationsByOrg :many
SELECT id, org_id, scheme_id, unit_id, email, full_name, role, token, status, expires_at, created_at FROM invitations
WHERE org_id = $1 AND status = 'pending'
ORDER BY created_at DESC
`

func (q *Queries) ListInvitationsByOrg(ctx context.Context, orgID uuid.UUID) ([]Invitation, error) {
	rows, err := q.db.Query(ctx, listInvitationsByOrg, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Invitation{}
	for rows.Next() {
		var i Invitation
		if err := rows.Scan(
			&i.ID,
			&i.OrgID,
			&i.SchemeID,
			&i.UnitID,
			&i.Email,
			&i.FullName,
			&i.Role,
			&i.Token,
			&i.Status,
			&i.ExpiresAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateInvitationStatus = `-- name: UpdateInvitationStatus :exec
UPDATE invitations SET status = $1 WHERE id = $2
`

type UpdateInvitationStatusParams struct {
	Status string    `json:"status"`
	ID     uuid.UUID `json:"id"`
}

func (q *Queries) UpdateInvitationStatus(ctx context.Context, arg UpdateInvitationStatusParams) error {
	_, err := q.db.Exec(ctx, updateInvitationStatus, arg.Status, arg.ID)
	return err
}

const updateInvitationToken = `-- name: UpdateInvitationToken :one
UPDATE invitations
SET token = $1, expires_at = $2
WHERE id = $3
RETURNING id, org_id, scheme_id, unit_id, email, full_name, role, token, status, expires_at, created_at
`

type UpdateInvitationTokenParams struct {
	Token     string    `json:"token"`
	ExpiresAt time.Time `json:"expires_at"`
	ID        uuid.UUID `json:"id"`
}

func (q *Queries) UpdateInvitationToken(ctx context.Context, arg UpdateInvitationTokenParams) (Invitation, error) {
	row := q.db.QueryRow(ctx, updateInvitationToken, arg.Token, arg.ExpiresAt, arg.ID)
	var i Invitation
	err := row.Scan(
		&i.ID,
		&i.OrgID,
		&i.SchemeID,
		&i.UnitID,
		&i.Email,
		&i.FullName,
		&i.Role,
		&i.Token,
		&i.Status,
		&i.ExpiresAt,
		&i.CreatedAt,
	)
	return i, err
}
