// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: levy.sql

package dbgen

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createLevyAccount = `-- name: CreateLevyAccount :one
INSERT INTO levy_accounts (unit_id, period_id, amount_cents, due_date)
VALUES ($1, $2, $3, $4)
RETURNING id, unit_id, period_id, amount_cents, paid_cents, status, due_date, paid_date, created_at, updated_at
`

type CreateLevyAccountParams struct {
	UnitID      uuid.UUID   `json:"unit_id"`
	PeriodID    uuid.UUID   `json:"period_id"`
	AmountCents int64       `json:"amount_cents"`
	DueDate     pgtype.Date `json:"due_date"`
}

func (q *Queries) CreateLevyAccount(ctx context.Context, arg CreateLevyAccountParams) (LevyAccount, error) {
	row := q.db.QueryRow(ctx, createLevyAccount,
		arg.UnitID,
		arg.PeriodID,
		arg.AmountCents,
		arg.DueDate,
	)
	var i LevyAccount
	err := row.Scan(
		&i.ID,
		&i.UnitID,
		&i.PeriodID,
		&i.AmountCents,
		&i.PaidCents,
		&i.Status,
		&i.DueDate,
		&i.PaidDate,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createLevyPayment = `-- name: CreateLevyPayment :one
INSERT INTO levy_payments (levy_account_id, amount_cents, payment_date, reference, bank_ref)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, levy_account_id, amount_cents, payment_date, reference, bank_ref, created_at
`

type CreateLevyPaymentParams struct {
	LevyAccountID uuid.UUID   `json:"levy_account_id"`
	AmountCents   int64       `json:"amount_cents"`
	PaymentDate   pgtype.Date `json:"payment_date"`
	Reference     string      `json:"reference"`
	BankRef       pgtype.Text `json:"bank_ref"`
}

func (q *Queries) CreateLevyPayment(ctx context.Context, arg CreateLevyPaymentParams) (LevyPayment, error) {
	row := q.db.QueryRow(ctx, createLevyPayment,
		arg.LevyAccountID,
		arg.AmountCents,
		arg.PaymentDate,
		arg.Reference,
		arg.BankRef,
	)
	var i LevyPayment
	err := row.Scan(
		&i.ID,
		&i.LevyAccountID,
		&i.AmountCents,
		&i.PaymentDate,
		&i.Reference,
		&i.BankRef,
		&i.CreatedAt,
	)
	return i, err
}

const createLevyPeriod = `-- name: CreateLevyPeriod :one
INSERT INTO levy_periods (scheme_id, label, amount_cents, due_date)
VALUES ($1, $2, $3, $4)
RETURNING id, scheme_id, label, amount_cents, due_date, created_at
`

type CreateLevyPeriodParams struct {
	SchemeID    uuid.UUID   `json:"scheme_id"`
	Label       string      `json:"label"`
	AmountCents int64       `json:"amount_cents"`
	DueDate     pgtype.Date `json:"due_date"`
}

func (q *Queries) CreateLevyPeriod(ctx context.Context, arg CreateLevyPeriodParams) (LevyPeriod, error) {
	row := q.db.QueryRow(ctx, createLevyPeriod,
		arg.SchemeID,
		arg.Label,
		arg.AmountCents,
		arg.DueDate,
	)
	var i LevyPeriod
	err := row.Scan(
		&i.ID,
		&i.SchemeID,
		&i.Label,
		&i.AmountCents,
		&i.DueDate,
		&i.CreatedAt,
	)
	return i, err
}

const getLevyAccount = `-- name: GetLevyAccount :one
SELECT id, unit_id, period_id, amount_cents, paid_cents, status, due_date, paid_date, created_at, updated_at FROM levy_accounts
WHERE id = $1
LIMIT 1
`

func (q *Queries) GetLevyAccount(ctx context.Context, id uuid.UUID) (LevyAccount, error) {
	row := q.db.QueryRow(ctx, getLevyAccount, id)
	var i LevyAccount
	err := row.Scan(
		&i.ID,
		&i.UnitID,
		&i.PeriodID,
		&i.AmountCents,
		&i.PaidCents,
		&i.Status,
		&i.DueDate,
		&i.PaidDate,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getLevyAccountByUnitAndPeriod = `-- name: GetLevyAccountByUnitAndPeriod :one
SELECT id, unit_id, period_id, amount_cents, paid_cents, status, due_date, paid_date, created_at, updated_at FROM levy_accounts
WHERE unit_id = $1 AND period_id = $2
LIMIT 1
`

type GetLevyAccountByUnitAndPeriodParams struct {
	UnitID   uuid.UUID `json:"unit_id"`
	PeriodID uuid.UUID `json:"period_id"`
}

func (q *Queries) GetLevyAccountByUnitAndPeriod(ctx context.Context, arg GetLevyAccountByUnitAndPeriodParams) (LevyAccount, error) {
	row := q.db.QueryRow(ctx, getLevyAccountByUnitAndPeriod, arg.UnitID, arg.PeriodID)
	var i LevyAccount
	err := row.Scan(
		&i.ID,
		&i.UnitID,
		&i.PeriodID,
		&i.AmountCents,
		&i.PaidCents,
		&i.Status,
		&i.DueDate,
		&i.PaidDate,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getLevyPaymentByReference = `-- name: GetLevyPaymentByReference :one
SELECT id, levy_account_id, amount_cents, payment_date, reference, bank_ref, created_at FROM levy_payments
WHERE reference = $1
LIMIT 1
`

func (q *Queries) GetLevyPaymentByReference(ctx context.Context, reference string) (LevyPayment, error) {
	row := q.db.QueryRow(ctx, getLevyPaymentByReference, reference)
	var i LevyPayment
	err := row.Scan(
		&i.ID,
		&i.LevyAccountID,
		&i.AmountCents,
		&i.PaymentDate,
		&i.Reference,
		&i.BankRef,
		&i.CreatedAt,
	)
	return i, err
}

const getLevyPeriod = `-- name: GetLevyPeriod :one
SELECT id, scheme_id, label, amount_cents, due_date, created_at FROM levy_periods
WHERE id = $1
LIMIT 1
`

func (q *Queries) GetLevyPeriod(ctx context.Context, id uuid.UUID) (LevyPeriod, error) {
	row := q.db.QueryRow(ctx, getLevyPeriod, id)
	var i LevyPeriod
	err := row.Scan(
		&i.ID,
		&i.SchemeID,
		&i.Label,
		&i.AmountCents,
		&i.DueDate,
		&i.CreatedAt,
	)
	return i, err
}

const listLevyAccountsByPeriod = `-- name: ListLevyAccountsByPeriod :many
SELECT la.id, la.unit_id, la.period_id, la.amount_cents, la.paid_cents, la.status, la.due_date, la.paid_date, la.created_at, la.updated_at, u.identifier AS unit_identifier, u.owner_name
FROM levy_accounts la
JOIN units u ON u.id = la.unit_id
WHERE la.period_id = $1
ORDER BY u.identifier
`

type ListLevyAccountsByPeriodRow struct {
	ID             uuid.UUID   `json:"id"`
	UnitID         uuid.UUID   `json:"unit_id"`
	PeriodID       uuid.UUID   `json:"period_id"`
	AmountCents    int64       `json:"amount_cents"`
	PaidCents      int64       `json:"paid_cents"`
	Status         string      `json:"status"`
	DueDate        pgtype.Date `json:"due_date"`
	PaidDate       pgtype.Date `json:"paid_date"`
	CreatedAt      time.Time   `json:"created_at"`
	UpdatedAt      time.Time   `json:"updated_at"`
	UnitIdentifier string      `json:"unit_identifier"`
	OwnerName      string      `json:"owner_name"`
}

func (q *Queries) ListLevyAccountsByPeriod(ctx context.Context, periodID uuid.UUID) ([]ListLevyAccountsByPeriodRow, error) {
	rows, err := q.db.Query(ctx, listLevyAccountsByPeriod, periodID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListLevyAccountsByPeriodRow{}
	for rows.Next() {
		var i ListLevyAccountsByPeriodRow
		if err := rows.Scan(
			&i.ID,
			&i.UnitID,
			&i.PeriodID,
			&i.AmountCents,
			&i.PaidCents,
			&i.Status,
			&i.DueDate,
			&i.PaidDate,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.UnitIdentifier,
			&i.OwnerName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listLevyPaymentsByAccount = `-- name: ListLevyPaymentsByAccount :many
SELECT id, levy_account_id, amount_cents, payment_date, reference, bank_ref, created_at FROM levy_payments
WHERE levy_account_id = $1
ORDER BY payment_date DESC
`

func (q *Queries) ListLevyPaymentsByAccount(ctx context.Context, levyAccountID uuid.UUID) ([]LevyPayment, error) {
	rows, err := q.db.Query(ctx, listLevyPaymentsByAccount, levyAccountID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []LevyPayment{}
	for rows.Next() {
		var i LevyPayment
		if err := rows.Scan(
			&i.ID,
			&i.LevyAccountID,
			&i.AmountCents,
			&i.PaymentDate,
			&i.Reference,
			&i.BankRef,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listLevyPeriodsByScheme = `-- name: ListLevyPeriodsByScheme :many
SELECT id, scheme_id, label, amount_cents, due_date, created_at FROM levy_periods
WHERE scheme_id = $1
ORDER BY due_date DESC
`

func (q *Queries) ListLevyPeriodsByScheme(ctx context.Context, schemeID uuid.UUID) ([]LevyPeriod, error) {
	rows, err := q.db.Query(ctx, listLevyPeriodsByScheme, schemeID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []LevyPeriod{}
	for rows.Next() {
		var i LevyPeriod
		if err := rows.Scan(
			&i.ID,
			&i.SchemeID,
			&i.Label,
			&i.AmountCents,
			&i.DueDate,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateLevyAccountPaid = `-- name: UpdateLevyAccountPaid :one
UPDATE levy_accounts
SET paid_cents = $2,
    status     = $3,
    paid_date  = $4
WHERE id = $1
RETURNING id, unit_id, period_id, amount_cents, paid_cents, status, due_date, paid_date, created_at, updated_at
`

type UpdateLevyAccountPaidParams struct {
	ID        uuid.UUID   `json:"id"`
	PaidCents int64       `json:"paid_cents"`
	Status    string      `json:"status"`
	PaidDate  pgtype.Date `json:"paid_date"`
}

func (q *Queries) UpdateLevyAccountPaid(ctx context.Context, arg UpdateLevyAccountPaidParams) (LevyAccount, error) {
	row := q.db.QueryRow(ctx, updateLevyAccountPaid,
		arg.ID,
		arg.PaidCents,
		arg.Status,
		arg.PaidDate,
	)
	var i LevyAccount
	err := row.Scan(
		&i.ID,
		&i.UnitID,
		&i.PeriodID,
		&i.AmountCents,
		&i.PaidCents,
		&i.Status,
		&i.DueDate,
		&i.PaidDate,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
