// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: collection_events.sql

package dbgen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createCollectionEvent = `-- name: CreateCollectionEvent :one
INSERT INTO collection_events (
    scheme_id,
    levy_account_id,
    actor_user_id,
    actor_role,
    event_type,
    note,
    promise_amount_cents,
    promise_date
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, scheme_id, levy_account_id, actor_user_id, actor_role, event_type, note, promise_amount_cents, promise_date, created_at
`

type CreateCollectionEventParams struct {
	SchemeID           uuid.UUID   `json:"scheme_id"`
	LevyAccountID      uuid.UUID   `json:"levy_account_id"`
	ActorUserID        pgtype.UUID `json:"actor_user_id"`
	ActorRole          string      `json:"actor_role"`
	EventType          string      `json:"event_type"`
	Note               pgtype.Text `json:"note"`
	PromiseAmountCents pgtype.Int8 `json:"promise_amount_cents"`
	PromiseDate        pgtype.Date `json:"promise_date"`
}

func (q *Queries) CreateCollectionEvent(ctx context.Context, arg CreateCollectionEventParams) (CollectionEvent, error) {
	row := q.db.QueryRow(ctx, createCollectionEvent,
		arg.SchemeID,
		arg.LevyAccountID,
		arg.ActorUserID,
		arg.ActorRole,
		arg.EventType,
		arg.Note,
		arg.PromiseAmountCents,
		arg.PromiseDate,
	)
	var i CollectionEvent
	err := row.Scan(
		&i.ID,
		&i.SchemeID,
		&i.LevyAccountID,
		&i.ActorUserID,
		&i.ActorRole,
		&i.EventType,
		&i.Note,
		&i.PromiseAmountCents,
		&i.PromiseDate,
		&i.CreatedAt,
	)
	return i, err
}

const listAttentionAccountsByOrg = `-- name: ListAttentionAccountsByOrg :many
SELECT
    la.id AS levy_account_id,
    lp.scheme_id,
    s.name AS scheme_name,
    la.unit_id,
    u.identifier AS unit_identifier,
    u.owner_name,
    la.amount_cents,
    la.paid_cents,
    la.due_date,
    la.status
FROM levy_accounts la
JOIN levy_periods lp ON lp.id = la.period_id
JOIN schemes s ON s.id = lp.scheme_id
JOIN units u ON u.id = la.unit_id
WHERE s.org_id = $1
  AND la.amount_cents > la.paid_cents
  AND la.due_date <= CURRENT_DATE
ORDER BY la.due_date ASC, (la.amount_cents - la.paid_cents) DESC
`

type ListAttentionAccountsByOrgRow struct {
	LevyAccountID  uuid.UUID   `json:"levy_account_id"`
	SchemeID       uuid.UUID   `json:"scheme_id"`
	SchemeName     string      `json:"scheme_name"`
	UnitID         uuid.UUID   `json:"unit_id"`
	UnitIdentifier string      `json:"unit_identifier"`
	OwnerName      string      `json:"owner_name"`
	AmountCents    int64       `json:"amount_cents"`
	PaidCents      int64       `json:"paid_cents"`
	DueDate        pgtype.Date `json:"due_date"`
	Status         string      `json:"status"`
}

func (q *Queries) ListAttentionAccountsByOrg(ctx context.Context, orgID uuid.UUID) ([]ListAttentionAccountsByOrgRow, error) {
	rows, err := q.db.Query(ctx, listAttentionAccountsByOrg, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAttentionAccountsByOrgRow{}
	for rows.Next() {
		var i ListAttentionAccountsByOrgRow
		if err := rows.Scan(
			&i.LevyAccountID,
			&i.SchemeID,
			&i.SchemeName,
			&i.UnitID,
			&i.UnitIdentifier,
			&i.OwnerName,
			&i.AmountCents,
			&i.PaidCents,
			&i.DueDate,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAttentionAccountsByScheme = `-- name: ListAttentionAccountsByScheme :many
SELECT
    la.id AS levy_account_id,
    lp.scheme_id,
    s.name AS scheme_name,
    la.unit_id,
    u.identifier AS unit_identifier,
    u.owner_name,
    la.amount_cents,
    la.paid_cents,
    la.due_date,
    la.status
FROM levy_accounts la
JOIN levy_periods lp ON lp.id = la.period_id
JOIN schemes s ON s.id = lp.scheme_id
JOIN units u ON u.id = la.unit_id
WHERE lp.scheme_id = $1
  AND la.amount_cents > la.paid_cents
  AND la.due_date <= CURRENT_DATE
ORDER BY la.due_date ASC, (la.amount_cents - la.paid_cents) DESC
`

type ListAttentionAccountsBySchemeRow struct {
	LevyAccountID  uuid.UUID   `json:"levy_account_id"`
	SchemeID       uuid.UUID   `json:"scheme_id"`
	SchemeName     string      `json:"scheme_name"`
	UnitID         uuid.UUID   `json:"unit_id"`
	UnitIdentifier string      `json:"unit_identifier"`
	OwnerName      string      `json:"owner_name"`
	AmountCents    int64       `json:"amount_cents"`
	PaidCents      int64       `json:"paid_cents"`
	DueDate        pgtype.Date `json:"due_date"`
	Status         string      `json:"status"`
}

func (q *Queries) ListAttentionAccountsByScheme(ctx context.Context, schemeID uuid.UUID) ([]ListAttentionAccountsBySchemeRow, error) {
	rows, err := q.db.Query(ctx, listAttentionAccountsByScheme, schemeID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAttentionAccountsBySchemeRow{}
	for rows.Next() {
		var i ListAttentionAccountsBySchemeRow
		if err := rows.Scan(
			&i.LevyAccountID,
			&i.SchemeID,
			&i.SchemeName,
			&i.UnitID,
			&i.UnitIdentifier,
			&i.OwnerName,
			&i.AmountCents,
			&i.PaidCents,
			&i.DueDate,
			&i.Status,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listCollectionEventsByAccountIDs = `-- name: ListCollectionEventsByAccountIDs :many
SELECT
    id,
    scheme_id,
    levy_account_id,
    actor_user_id,
    actor_role,
    event_type,
    note,
    promise_amount_cents,
    promise_date,
    created_at
FROM collection_events
WHERE levy_account_id = ANY($1::uuid[])
ORDER BY created_at DESC
`

func (q *Queries) ListCollectionEventsByAccountIDs(ctx context.Context, dollar_1 []uuid.UUID) ([]CollectionEvent, error) {
	rows, err := q.db.Query(ctx, listCollectionEventsByAccountIDs, dollar_1)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CollectionEvent{}
	for rows.Next() {
		var i CollectionEvent
		if err := rows.Scan(
			&i.ID,
			&i.SchemeID,
			&i.LevyAccountID,
			&i.ActorUserID,
			&i.ActorRole,
			&i.EventType,
			&i.Note,
			&i.PromiseAmountCents,
			&i.PromiseDate,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
